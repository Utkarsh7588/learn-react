import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#32a852', // Example primary color
    },
    secondary: {
      main: '#ff4081', // Example secondary color
    },
  },
});

export default theme; 